<Window x:Class="ProjectPRN.AdminMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPRN"
        mc:Ignorable="d"
              Title="Admin Main Menu" Height="600" Width="800" Loaded="Window_Loaded">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Label x:Name="txtWelcome" Content="Welcome!" FontSize="16"/>
            <Label x:Name="labelTime" Content="00:00:00" FontSize="16" Margin="20,0,0,0"/>
            <Button x:Name="btnLogout" Content="Logout" Width="80" Margin="20,0,0,0" Click="BtnLogout_Click" HorizontalAlignment="Right"/>
        </StackPanel>

        <!-- Navigation Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button x:Name="btnHome" Content="Home" Width="100" Margin="0,0,10,0" Click="BtnHome_Click"/>
            <Button x:Name="btnEmployee" Content="Employees" Width="100" Margin="0,0,10,0" Click="BtnEmployee_Click"/>
            <Button x:Name="btnStatic" Content="Statistics" Width="100" Margin="0,0,10,0" Click="BtnStatic_Click"/>
            <Button x:Name="btnProduct" Content="Products" Width="100" Margin="0,0,10,0" Click="BtnProduct_Click"/>
        </StackPanel>

        <!-- Content Area -->
        <Frame x:Name="panelDetail" Grid.Row="2" NavigationUIVisibility="Hidden" Margin="10,20,10,0" />
    </Grid>
</Window>

